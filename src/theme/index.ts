import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#555",
      contrastText: "#ffffff",
    },
    dadago: {
      main: "#ff6633",
      contrastText: "#ffffff",
    },
  },
});

export default theme;
